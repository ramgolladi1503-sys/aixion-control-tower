# Copy this file to keystore.properties and fill it locally.
# Never commit keystore.properties or the keystore file.

ANDROID_KEYSTORE_FILE=release.keystore
ANDROID_KEYSTORE_PASSWORD=replace-with-local-password
ANDROID_KEY_ALIAS=replace-with-key-alias
ANDROID_KEY_PASSWORD=replace-with-key-password
