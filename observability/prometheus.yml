global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: aixion-mission-control
    metrics_path: /agent/runs/metrics
    static_configs:
      - targets:
          - api:8000
